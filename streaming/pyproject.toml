[project]
name = "deng-streaming"
version = "0.1.0"
description = "Streaming exercises for DENG: Redpanda + Flink + Dinky"
requires-python = ">=3.12"
dependencies = [
    "confluent-kafka>=2.6.0",
    "faker>=30.0.0",
    "ipykernel>=6.29.0",
]

[tool.uv]
package = false
